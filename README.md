# BrotherHood

One-page site for **BrotherHood** on Robinhood Chain. Static, no build.

```bash
node serve.js   # http://localhost:8974
```

Pending: contract address (`#ca-value`), Buy link (`#buy`), X link (`#x`).
